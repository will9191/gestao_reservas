namespace Reservation.Repository
{
    public class ReserveRepository
    {
        public string NewReserve(Model.ReserveModel reserve)
        {
            // Simulate saving the reservation and returning a unique identifier
            return string.Empty;
        }

        public string GetReserveStatus(Model.AddressModel address)
        {
            // Simulate saving the reservation and returning a unique identifier
            return string.Empty;
        }
        public string BookReserve(Model.AddressModel address)
        {
            // Simulate saving the reservation and returning a unique identifier
            return string.Empty;
        }

    }
}
